---
name: Never run builds
description: User builds themselves — never run cmake/ninja/make build commands
type: feedback
---

Never run build commands. The user handles building themselves.

**Why:** User finds repeated build attempts annoying and wasteful of time.
**How to apply:** Make code changes, explain what was done, let the user build and report errors.
